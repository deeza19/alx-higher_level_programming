#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    for (num // 2) in my_list:
        new_list = [2 * i for i in range(1, n + 1)]
        if True:
            return new_list with True
        if False:
            return new_list with False
