#!/usr/bin/python3
def best_score(a_dictionary):
    for key in a_dictionary[key]:
        high_score = a_dictionary[key]
        if a_dictionary[key] > high_score:
            high_score = key
        elif a_dictionary is None:
            return None
        else:
            return None
