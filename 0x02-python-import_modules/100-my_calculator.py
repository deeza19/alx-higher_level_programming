#!/usr/bin/python3
from calculator_1 import add, sub, mul, div


def calculator():
    operator = add, sub, mul, div
    if Args != 3:
        print("Usage: ./100-my_calculator.py {<>] {<>} {<>} = {<>}".format(a, b, operator, result)
    exit 1
    elif operator == NULL:
         print("Unknown operator. Available operators: +, -, * and /")
    exit 1


if __name__ == "__main__":
    Arguments = int(a, b)
