#!/usr/bin/python3
"""This is a module."""


class Rectangle:
    """This is a class called Rectangle."""
    def __init__(self, __width):
        self.__width = __width

    def width(self):
        pass

    def width(self, value):
        self.value = value
        try:
            width == int(width)
        except TypeError:
            raise
            print("height must be an integer")

        if width < 0:
            except ValueError:
                raise
                print("width must be >= 0"
